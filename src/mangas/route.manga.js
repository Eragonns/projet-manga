// import express from "express";

// import validate from "../middlewares/validation.middleware.js";
// import { MangaBodySchema, MangaParamsSchema } from "../schemas/manga.schema.js";
// import * as mangasController from "../controllers/mangas/controller.manga.js";

// const router = express.Router();

// router.route("/").get(mangasController.getAll);

// router
//   .route("/:id")
//   .get(validate({ paramsSchema: MangaParamsSchema }), mangasController.get);

// export default router;
